

mixin Tiger {
  void speak() { 
    system.prettyPrint("[Tiger] This is tigher speak method invocation !");  
  }
}

mixin Cat {
  void speak() { 
    system.prettyPrint("[Cat] This is tigher speak method invocation !");  
  }
}

mixin Eagle on Tiger, Cat {
  void speak() { 
    super.speak();
    system.prettyPrint("[Eage] This is eagle speak method invocation !"); 
  }
}

class Zoo with Tiger, Cat, Eagle { }

int main() {

  auto z = new Zoo();

  z.speak();
  
  return 0;
}
